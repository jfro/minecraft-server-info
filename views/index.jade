h1 Someone's Minecraft Server

#content
	#user-list
		table(cellspacing=0, cellpadding=5, border=0)
			tr
				th Online
				th User
				th 
			- each user in users
				- var onlineString = user.online ? 'online' : '';
				tr(class=onlineString)
					td #{onlineString}
					td #{user.username}
					td 
						abbr.timeago(title=user.last_action_date) #{user.last_action_date}
						